[package]
name = "sprak-plugin-sdk"
version = "0.1.0"
edition = "2024"
description = "Sprak Capture Native ABI v2 插件 SDK"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
members = ["examples/nativeRewrite"]
resolver = "3"
