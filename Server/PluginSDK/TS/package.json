{
  "name": "@sprak/capture-plugin-sdk",
  "version": "2.0.0",
  "description": "Sprak Capture TypeScript 插件作者 SDK 与本地宿主模拟器",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": { ".": { "types": "./dist/src/index.d.ts", "import": "./dist/src/index.js" } },
  "files": ["dist/src", "dist/examples", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/packageExamples.mjs",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
